---
interface Props {
  tone?: 'tint' | 'grey' | 'paper';
  class?: string;
  id?: string;
}

const { tone = 'paper', class: className, id } = Astro.props;
---

<section id={id} class:list={['section-band', `section-band--${tone}`, className]} data-reveal>
  <slot />
</section>
